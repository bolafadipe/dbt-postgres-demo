version: 2

sources:
  - name: public
    database: dvdrental  
    tables:
      - name: address
        columns:
          - name: address_id
            description: Primary key of the address table
            tests:
              - unique
              - not_null
         